# src/csv_analyzer/cli.py
import argparse

def build_parser() -> argparse.ArgumentParser:
    """Define argumentos: --input, --format, --output, --agente, --estado, --desde, --hasta."""

def main() -> int:
    "Orquesta loader + metrics + output. Retornna codigo de salida (0 o 1)."    